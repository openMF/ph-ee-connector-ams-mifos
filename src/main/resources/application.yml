camel:
  server-port: 5006
  springboot:
    main-run-controller: true
  dataformat:
    json-jackson:
      auto-discover-object-mapper: true

dfspids: "gorilla"
TENANT_CONFIGS: "{\"gorilla\":[{\"Operation\":\"abc\",\"PaymentTypeCode\":\"pt1\",\"FineractId\":1}]}"
spring:
  profiles:
    active: "bb,fin12,dev"

fineract:
  api-url: "https://fynams.sandbox.fynarfin.io/fineract-provider/api/v1"
  datatable-query-api: "/datatables/account_mapping/query"
  column-filter: "iban"
  result-columns: "conversion_account_id,disposal_account_id"
  internal-id-column: "internal_account_id"
  incoming-money-api: "/savingsaccounts/"
  locale: "en"

zeebe:
  client:
    max-execution-threads: 1000
    evenly-allocated-max-jobs: 1000
#    max-execution-threads: 100
#    number-of-workers: 8
#    evenly-allocated-max-jobs: "#{${zeebe.client.max-execution-threads} / ${zeebe.client.number-of-workers}}"
  broker:
    contactpoint: "zeebe-zeebe-gateway:26500"

#ams:
#  local:
#    server-cert-check: false
#    enabled: true

#ams:
#  local:
#    server-cert-check: false
#    enabled: true

ams:
  local:
    server-cert-check: false
    enabled: true

interop-party-registration:
  enabled: false

logging:
  level:
    root: INFO
  pattern:
    console: "%clr(%d{dd-MM-yyyy HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr([%35.35t]){faint} %clr(%-28.28logger{28}){cyan} %clr(:){faint}%X{BUSINESS-LOG} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"